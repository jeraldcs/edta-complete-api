#!/bin/sh
# Strip Cursor co-author trailer from commit messages (local IDE/CLI/cloud paths).
sed -i.bak '/cursoragent@cursor\.com/d' "$1" 2>/dev/null || sed -i '' '/cursoragent@cursor\.com/d' "$1"
rm -f "$1.bak"
